export const ValidatorErrors = {
    required: 'required',
    pattern: 'pattern',
    minlength: 'minlength',
    maxlength: 'maxlength',
    email: 'email',
    passwordMismatch: 'passwordMismatch',
    requiredTrue: 'requiredTrue',
    ucn: 'ucn',
    min: 'min',
    max: 'max',
}